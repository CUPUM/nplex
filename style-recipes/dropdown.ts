import { defineSlotRecipe } from '@pandacss/dev';

export const dropdownRecipe = defineSlotRecipe({
	slots: ['root', 'item'],
	className: 'dropdown',
	base: {},
	variants: {},
	defaultVariants: {},
});
