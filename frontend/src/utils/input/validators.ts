/**
 * Define here validation patterns for field inputs or user-created data.
 *
 * Validators are bi-purpose:
 *
 * - They serve to validate the passed values;
 * - They can also be used to auto-format values (ex.: on:input).
 */

const patterns: Record<string, RegExp> = {};

function formatValue(value: string, pattern: RegExp) {
	// return formattedValue;
}
