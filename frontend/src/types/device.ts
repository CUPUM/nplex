/**
 * Enum of possible client device type detected by the server, usable for SSR or client-side UI adjustments.
 */
export enum Device {
	Mobile = 'mobile',
	Desktop = 'desktop',
	Tablet = 'tablet',
}
