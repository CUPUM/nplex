interface DragndropOptions {}

const DRAGNDROP_EVENTS = {
	Start: 'dnd:start',
	Cancel: 'dnd:cancel',
	Consider: 'dnd:consider',
};

/**
 * Drag and drop helper for reorderable lists and flip animations.
 */
export function dragndrop(element: HTMLElement): SvelteActionReturnType {}
