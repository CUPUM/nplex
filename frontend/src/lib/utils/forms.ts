/**
 * Take avalidation schema and prepare field attributes such as name and constriants that can be
 * applied to components or inputs through destructuring. Ex.:
 *
 * <Field {...formAttributes.short_title} />
 *
 * Ensures symmetry of constraints and input names across client and server.
 */
export function makeForm() {
	return {
		// dirtyValues
		// dirty
		// data
	};
}
