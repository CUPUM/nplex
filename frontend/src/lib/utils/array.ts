/**
 * Move given item(s) to a new place in the array. This does not modify the original array.
 *
 * @returns A new reordered array.
 */
export function moveItem<T>(array: T[], toIndex: number, ...fromIndex: number[]) {
	const ordered = [...array];
}
